#include <stdio.h>

/**
 * main - Entry point of the program
 * @void: No parameters expected
 *
 * Return: 0 on success
 */
int main(void) {
printf("0\n");
/* No command-line arguments (excluding program name) */
return (0);
}
