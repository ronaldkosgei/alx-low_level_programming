#include "main.h"
#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * Return: Always 0.
 */
int main(void)
{
int a = 2;
int b = 3;

printf("%d * %d = %d\n", a, b, mul(a, b));

return (0);
}
